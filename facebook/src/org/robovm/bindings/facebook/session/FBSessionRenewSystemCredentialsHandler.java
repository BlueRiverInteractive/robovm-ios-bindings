
package org.robovm.bindings.facebook.session;

/** Block type used to define blocks called for system credential renewals. */
public interface FBSessionRenewSystemCredentialsHandler {
// void invoke(ACAccountCredentialRenewResult result, NSError error);
}
