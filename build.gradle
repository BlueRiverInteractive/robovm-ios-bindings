buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
        maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
    }
    dependencies {
        classpath 'com.github.jtakakura:gradle-robovm-plugin:0.0.9-SNAPSHOT'
    }
}

allprojects {
    apply plugin: "eclipse"
    apply plugin: "idea"
    apply plugin: "java"
    apply plugin: "robovm"

    version = '1.0.0-SNAPSHOT'

    ext {
        roboVMVersion = '0.0.13-SNAPSHOT'
    }

    dependencies {
        compile "org.robovm:robovm-rt:${roboVMVersion}"
        compile "org.robovm:robovm-cocoatouch:${roboVMVersion}"
    }

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
    }
}

project(':adcolony') {
    ext {
        mainClassName = 'org.robovm.bindings.adcolony.sample.Sample'
    }
}

project(':admob') {
    ext {
        mainClassName = 'org.robovm.bindings.admob.sample.Sample'
    }
}

project(':appirater') {
    ext {
        mainClassName = 'org.robovm.bindings.appirater.sample.Sample'
    }
}

project(':chartboost') {
    ext {
        mainClassName = 'org.robovm.bindings.chartboost.sample.Sample'
    }
}

project(':crashlytics') {
    ext {
        mainClassName = 'org.robovm.bindings.crashlytics.sample.Sample'
    }
}

project(':facebook') {
    ext {
        mainClassName = 'org.robovm.bindings.facebook.manager.sample.Sample'
    }
}

project(':flurry') {
    ext {
        mainClassName = 'org.robovm.bindings.flurry.sample.Sample'
    }
}

project(':gamecenter') {
    ext {
        mainClassName = 'org.robovm.bindings.gamecenter.Sample'
    }
}

project(':google-analytics') {
    ext {
        mainClassName = 'org.robovm.bindings.googleanalytics.sample.Sample'
    }
}

project(':google-play-game-services') {
    ext {
        mainClassName = 'org.robovm.bindings.gpgs.sample.Sample'
    }
}

project(':greystripe') {
    ext {
        mainClassName = 'org.robovm.bindings.greystripe.sample.Sample'
    }
}

project(':in-app-purchase') {
    ext {
        mainClassName = 'org.robovm.bindings.inapppurchase.sample.Sample'
    }
}

project(':massageui') {
    ext {
        mainClassName = 'org.robovm.apple.messageui.sample.Sample'
    }
}

project(':mopub') {
    ext {
        mainClassName = 'org.robovm.bindings.mopub.sample.Sample'
    }
}

project(':openkit') {
    ext {
        mainClassName = 'org.robovm.bindings.openkit.sample.Sample'
    }
}

project(':playhaven') {
    ext {
        mainClassName = 'org.robovm.bindings.playhaven.sample.Sample'
    }
}

project(':tapjoy') {
    ext {
        mainClassName = 'org.robovm.bindings.tapjoy.sample.Sample'
    }
}

project(':thinkgaming') {
    ext {
        mainClassName = 'org.robovm.bindings.thinkgaming.sample.Sample'
    }
}

project(':vungle') {
    ext {
        mainClassName = 'org.robovm.bindings.vungle.sample.Sample'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}

